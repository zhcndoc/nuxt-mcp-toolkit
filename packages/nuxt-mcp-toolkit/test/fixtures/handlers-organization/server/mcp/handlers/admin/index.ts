import { defineMcpHandler } from '../../../../../../../src/runtime/server/types'

export default defineMcpHandler({
  description: 'Admin handler — folder convention auto-attaches its tools/prompts.',
})
