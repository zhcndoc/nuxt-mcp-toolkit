/**
 * The protocol revision this toolkit targets.
 *
 * The SDK keeps it internal — its `LATEST_PROTOCOL_VERSION` still names the
 * last 2025-era revision — so it is restated here for anything that needs to
 * name the modern wire format explicitly.
 */
export const MODERN_PROTOCOL_VERSION = '2026-07-28'
