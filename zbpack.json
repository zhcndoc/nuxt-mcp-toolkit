{
  "app_dir": "/",
  "build_command": "pnpm run dev:prepare && cd apps/docs && NITRO_PRESET=static pnpm run generate",
  "ignore_dockerfile": true,
  "install_command": "npm install -g pnpm && pnpm install",
  "output_dir": "apps/docs/.output/public"
}
