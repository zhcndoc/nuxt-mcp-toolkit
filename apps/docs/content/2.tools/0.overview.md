---
title: 工具
description: 使用 Zod 校验和类型安全创建 MCP 工具。
navigation:
  title: 概览
  icon: i-lucide-wrench
links:
  - label: Schema 与 handler
    icon: i-lucide-braces
    to: /tools/schema-handler
    color: neutral
    variant: subtle
  - label: 注解
    icon: i-lucide-badge-info
    to: /tools/annotations
    color: neutral
    variant: subtle
  - label: 错误与缓存
    icon: i-lucide-shield
    to: /tools/errors-caching
    color: neutral
    variant: subtle
  - label: 分组与高级
    icon: i-lucide-tags
    to: /tools/groups-organization
    color: neutral
    variant: subtle
seo:
  title: 工具
  description: 使用 Zod 校验和完整的 TypeScript 支持创建 MCP 工具。
---

## 什么是工具？

工具是 AI 助手可以调用来执行操作或检索信息的函数。它们接收经过验证的输入参数，并返回结构化结果。

::prompt
---
description: 搭建一个新的 MCP 工具
icon: i-lucide-wrench
actions:
  - copy
  - cursor
  - windsurf
---

在我的 Nuxt 应用中使用 @nuxtjs/mcp-toolkit 创建一个新的 MCP 工具。

- 在 server/mcp/tools/ 中创建一个文件（例如 server/mcp/tools/my-tool.ts）
- 使用 defineMcpTool（自动导入）并提供 description 和 handler
- 使用 `import { z } from 'zod'` 导入 Zod，并在 inputSchema 中定义输入参数（例如 z.string().describe('...')）
- handler 接收经过验证的输入，并返回 string、number、boolean、object 或完整的 CallToolResult
- 使用 h3 的 createError({ statusCode, message }) 抛出错误以处理错误情况
- 名称和标题会根据文件名自动生成（例如 my-tool.ts → name: 'my-tool', title: 'My Tool'）
- 为行为提示添加注解（readOnlyHint、destructiveHint、idempotentHint、openWorldHint）
- 使用子目录自动推断分组（例如 tools/admin/delete-user.ts → group: 'admin'）

文档：https://mcp-toolkit.nuxt.dev/tools/overview

::

## 基础工具定义

下面是一个简单的工具，会回显一条消息：

```typescript [server/mcp/tools/echo.ts]
import { z } from 'zod'
import { defineMcpTool } from '@nuxtjs/mcp-toolkit/server'

export default defineMcpTool({
  name: 'echo',
  description: '回显一条消息',
  inputSchema: {
    message: z.string().describe('要回显的消息'),
  },
  handler: async ({ message }) => `回显：${message}`,
})
```

## 自动生成的名称和标题

你可以省略 `name` 和 `title`——它们将从文件名自动生成：

```typescript [server/mcp/tools/list-documentation.ts]
import { defineMcpTool } from '@nuxtjs/mcp-toolkit/server'

export default defineMcpTool({
  // name 和 title 会根据文件名自动生成：
  // name: 'list-documentation'
  // title: 'List Documentation'
  description: '列出所有文档文件',
  handler: async () => {
    // ...
  },
})
```

文件名 `list-documentation.ts` 会自动变为：
- `name`: `list-documentation`（kebab-case）
- `title`: `List Documentation`（标题式大小写）

你仍然可以显式提供 `name` 或 `title` 来覆盖自动生成的值。

## 工具结构

一个工具定义由以下部分组成：

::code-group

```typescript [Required Fields]
export default defineMcpTool({
  name: 'tool-name',        // 唯一标识符（可选 - 将从文件名自动生成）
  inputSchema: { ... },      // 用于输入校验的 Zod schema
  handler: async (args) => {
    return 'result'          // string、number、boolean、object 或 CallToolResult
  },
})
```

```typescript [Optional Fields]
export default defineMcpTool({
  name: 'tool-name',              // 可选 - 将从文件名自动生成
  title: 'Tool Title',            // 可选 - 将从文件名自动生成
  description: '工具描述',  // 工具的作用
  inputSchema: { ... },            // 可选 - 用于输入校验的 Zod schema
  outputSchema: { ... },            // 用于结构化输出的 Zod schema
  annotations: { ... },             // 面向客户端的行为提示
  inputExamples: [{ ... }],         // 具体的使用示例
  handler: async (args) => { ... },
})
```

::

## 深入指南

工具文档被拆分为几个聚焦的页面，这样目录就能保持清晰易读。

::card-group
  :::card
  ---
  icon: i-lucide-braces
  title: Schema、handler 与返回值
  to: /tools/schema-handler
  color: neutral
  ---
  Zod `inputSchema` / `outputSchema`、handler 返回类型，以及媒体辅助函数。
  :::
  :::card
  ---
  icon: i-lucide-badge-info
  title: 注解与输入示例
  to: /tools/annotations
  color: neutral
  ---
  面向客户端的提示，以及用于让模型更易理解的 `inputExamples` 示例。
  :::
  :::card
  ---
  icon: i-lucide-shield
  title: 错误与响应缓存
  to: /tools/errors-caching
  color: neutral
  ---
  H3 和普通错误，Nitro `cache` 选项。
  :::
  :::card
  ---
  icon: i-lucide-tags
  title: 分组、文件与动态注册
  to: /tools/groups-organization
  color: neutral
  ---
  `group` / `tags`、磁盘上的布局、`enabled` 守卫，以及后续步骤。
  :::
::
