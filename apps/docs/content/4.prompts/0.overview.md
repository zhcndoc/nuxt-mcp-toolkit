---
title: 提示词
description: 为 AI 助手创建可复用的提示词，并支持可选参数。
navigation:
  title: 概览
  icon: i-lucide-message-square
links:
  - label: 编写与结构
    icon: i-lucide-pen-line
    to: /prompts/authoring
    color: neutral
    variant: subtle
  - label: 输入、处理器与消息
    icon: i-lucide-layers
    to: /prompts/input-handler-messages
    color: neutral
    variant: subtle
  - label: 模式与高级用法
    icon: i-lucide-line-chart
    to: /prompts/patterns-advanced
    color: neutral
    variant: subtle
seo:
  title: 提示词
  description: 创建可复用的 MCP 提示词，支持可选参数和完整的类型安全。
---

## 什么是提示词？

提示词是可复用的消息模板，可供 AI 助手使用。它们可以包含动态参数，并返回预先格式化的消息。

::prompt
---
description: 为新的 MCP 提示词搭建脚手架
icon: i-lucide-message-square
actions:
  - copy
  - cursor
  - windsurf
---

使用 @nuxtjs/mcp-toolkit 在我的 Nuxt 应用中创建一个新的 MCP 提示词。

- 在 server/mcp/prompts/ 中创建一个文件（例如 server/mcp/prompts/review-code.ts）
- 使用 defineMcpPrompt（自动导入）并提供描述和处理器
- 处理器可以返回一个字符串（会自动包装为用户消息）或一个包含 messages 数组的完整 GetPromptResult
- 在 inputSchema 中使用 `import { z } from 'zod'` 定义参数（提示词参数必须是字符串）
- 使用 role 选项（'user' 或 'assistant'）来控制字符串返回时的默认角色
- 使用 completable() 为参数提供自动补全建议
- 返回多个消息以创建对话流程
- 名称和标题会根据文件名自动生成
- 当在聊天中输入 / 时，提示词会出现在 Cursor 和 VS Code 中

文档：https://mcp-toolkit.nuxt.dev/prompts/overview

::

## 为什么使用提示词？

与临时性的指令相比，MCP 提示词提供了几个优势：

::card-group
  :::card
  ---
  icon: i-lucide-repeat
  title: 可复用性
  ---
  定义一次，到处使用。在团队中共享提示词，以获得一致的 AI 交互。
  :::

  :::card
  ---
  icon: i-lucide-check-square
  title: 标准化
  ---
  确保特定任务（如代码审查或文档编写）具有一致的格式和上下文。
  :::

  :::card
  ---
  icon: i-lucide-settings-2
  title: 自定义
  ---
  使用参数将提示词适配到不同上下文，同时保持结构不变。
  :::

  :::card
  ---
  icon: i-lucide-plug
  title: IDE 集成
  ---
  提示词会出现在 Cursor、VS Code 和 Visual Studio 中，便于在开发过程中快速访问。
  :::
::

## IDE 集成

当你的 MCP 服务器已连接时，MCP 提示词可以与现代开发环境无缝集成，提示词会直接在你的 IDE 中可用。

### 在 Cursor / VS Code 中使用提示词

1. **Type `/`** — 在聊天中输入 `/` 以查看所有可用的 MCP 提示词。
2. **Select a prompt** — 从列表中选择一个提示词（例如 `local-mcp/setup-mcp-server`）。
3. **Fill arguments** — 对于带参数的提示词，会弹出一个对话框来收集在你的 `inputSchema` 中定义的值。
4. **Send** — 提示词的消息会被插入到对话中，就像你亲自输入的一样。

Prompts surface with the prefix `<server-name>/<prompt-name>`, so renaming your handler in `defineMcpHandler({ name })` changes how they appear in clients.

## Going further

::card-group
  :::card
  ---
  icon: i-lucide-pen-line
  title: 编写与结构
  to: /prompts/authoring
  color: neutral
  ---
  自动生成的名称、简单提示词、默认角色和提示词结构。
  :::
  :::card
  ---
  icon: i-lucide-layers
  title: 输入、处理器与消息
  to: /prompts/input-handler-messages
  color: neutral
  ---
  Zod 参数、completable 自动补全、返回类型和多消息对话。
  :::
  :::card
  ---
  icon: i-lucide-line-chart
  title: 模式与高级用法
  to: /prompts/patterns-advanced
  color: neutral
  ---
  真实世界示例、分组、类型安全、最佳实践和条件注册。
  :::
  :::card
  ---
  icon: i-lucide-message-square
  title: 提示词示例
  to: /examples/prompt-examples
  color: neutral
  ---
  你可以复制的代码审查、文档、邮件和提交信息提示词。
  :::
::

## 下一步

- [编写与结构](/prompts/authoring) — 编写你的第一个提示词。
- [工具](/tools/overview) — 将提示词与 AI 可执行的操作配对。
- [资源](/resources/overview) — 将上下文与你的提示词一同提供。
