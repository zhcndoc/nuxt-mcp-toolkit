---
title: 应用
description: 构建交互式 UI 小部件，让 AI 主机与聊天内容并排渲染——以 Vue SFC 编写，作为 MCP UI 资源提供。
navigation:
  title: 概览
  icon: i-lucide-app-window
links:
  - label: 编写
    icon: i-lucide-code-2
    to: /apps/authoring
    color: neutral
    variant: subtle
  - label: useMcpApp 桥接
    icon: i-lucide-message-circle
    to: /apps/use-mcp-app
    color: neutral
    variant: subtle
  - label: CSP 与管线
    icon: i-lucide-shield
    to: /apps/csp-and-wiring
    color: neutral
    variant: subtle
  - label: 测试与发布
    icon: i-lucide-rocket
    to: /apps/testing-publishing
    color: neutral
    variant: subtle
seo:
  title: MCP 应用
  description: 将 MCP UI 应用编写为 Vue 单文件组件。自动打包，从真实的 Nuxt 端点提供，并在兼容 MCP 应用的主机内渲染。
---

## 什么是 MCP 应用？

MCP 应用是由 MCP 工具返回的 **交互式 HTML 小部件**，并由兼容的主机以内联方式渲染。你的工具不会流式返回文本，而是提供一个用户可以阅读、滚动、筛选和点击的小型 UI——通过一个带类型的消息桥接与服务器重新连接。

它们遵循 [MCP UI 提案（SEP-1865）](https://modelcontextprotocol.io)：工具返回一个由 `ui://` 引用的 `text/html;profile=mcp-app` 资源，主机将其加载到一个沙箱化的 `iframe` 中，然后 iframe 通过 `postMessage` 进行回传通信。

`@nuxtjs/mcp-toolkit` 让这种编写体验就像在写一个普通的 Nuxt 页面：

- 每个应用在 `app/mcp/` 中对应一个 **Vue SFC**。
- `<script setup>` 中的 `defineMcpApp` 宏声明工具、schema 和服务器处理程序。
- `useMcpApp()` 可组合函数为你提供响应式数据、主机上下文，以及到主机的类型化桥接。
- 该工具包会在构建时将 SFC 打包为一个自包含的 HTML 文件，并通过你的 MCP 端点提供它。

::prompt
---
description: 搭建一个新的 MCP 应用（Vue SFC）
icon: i-lucide-app-window
actions:
  - copy
  - cursor
  - windsurf
---

使用 @nuxtjs/mcp-toolkit 在我的 Nuxt 应用中创建一个新的 MCP 应用。

- 在 app/mcp/ 中创建一个 Vue SFC（例如 app/mcp/color-picker.vue）
- 在 <script setup lang="ts"> 中使用 defineMcpApp({ description, inputSchema, handler })
- inputSchema 使用 Zod；handler 在服务端运行并返回 { structuredContent }
- handler 可以通过 $fetch 调用任意 Nuxt API 路由——将繁重的数据工作留在服务器端
- 在模板中，调用 useMcpApp<TPayload>() 以获取 { data, loading, hostContext, sendPrompt, callTool, openLink }
- data 会在首次渲染时从 structuredContent 注入——无需额外请求
- 使用 sendPrompt(text) 将后续内容推送到聊天中，从而触发另一个工具/应用
- 使用 callTool(name, params) 重新调用一个 MCP 工具并就地刷新数据
- 使用 openLink(url) 请求主机在 iframe 外部打开一个 URL
- 如果你加载图片或调用外部 API，请添加 CSP 白名单 csp: { resourceDomains, connectDomains }
- 保持布局流式自适应（不要固定高度）；主机通常会以可变宽度内联渲染 iframe

文档：https://mcp-toolkit.nuxt.dev/apps/overview

::

## 指南

::card-group
  :::card
  ---
  icon: i-lucide-code-2
  title: 编写与 defineMcpApp
  to: /apps/authoring
  color: neutral
  ---
  快速开始、`app/mcp/` 布局、宏、服务器处理程序和共享类型。
  :::
  :::card
  ---
  icon: i-lucide-message-circle
  title: useMcpApp() 桥接
  to: /apps/use-mcp-app
  color: neutral
  ---
  `data`、`hostContext`、`sendPrompt`、`callTool` 和 `openLink` 在 iframe 内的使用。
  :::
  :::card
  ---
  icon: i-lucide-shield
  title: CSP 与构建管线
  to: /apps/csp-and-wiring
  color: neutral
  ---
  白名单域名、`openai/widgetCSP`，以及 HTML 包是如何接线的。
  :::
  :::card
  ---
  icon: i-lucide-rocket
  title: 测试与发布
  to: /apps/testing-publishing
  color: neutral
  ---
  本地开发、哪些主机可用、HTTPS 部署、ChatGPT 和 Claude。
  :::
  :::card
  ---
  icon: i-lucide-book-marked
  title: 模式与限制
  to: /apps/patterns-reference
  color: neutral
  ---
  UI 模式、你不能在 iframe 中做什么，以及一个简洁的 API 表。
  :::
::

本节其余部分假定你已经熟悉 [工具](/tools/overview)（Zod、处理程序），并且已经阅读了 [连接](/getting-started/connection)，了解开发和生产环境中的 MCP URL。
