---
title: 结构与选项
description: 必填字段以及 defineMcpHandler 的每个选项，包括 tools、resources 和 prompts。
navigation:
  icon: i-lucide-sliders-horizontal
seo:
  title: 处理器结构与选项
  description: defineMcpHandler 字段、route、middleware、experimental_codeMode、tools、resources、prompts。
---

## 处理器结构

一个处理器定义由以下内容组成：

::code-group

```typescript [必填字段]
import { defineMcpHandler } from '@nuxtjs/mcp-toolkit/server'

export default defineMcpHandler({
  name: 'handler-name',  // 唯一标识符
})
```

```typescript [可选字段]
import { defineMcpHandler } from '@nuxtjs/mcp-toolkit/server'

export default defineMcpHandler({
  name: 'handler-name',
  version: '1.0.0',              // 处理器版本
  route: '/mcp/custom',          // 自定义路由
  browserRedirect: '/',          // 浏览器重定向 URL
  middleware: async (event) => { ... }, // 请求拦截
  tools: [ ... ],                 // 工具数组
  resources: [ ... ],            // 资源数组
  prompts: [ ... ],               // 提示词数组
  experimental_codeMode: true,    // 启用代码模式（实验性）
})
```

::

## 处理器选项

### `name`（必需）

处理器的唯一标识符。`name` 决定处理器将挂载到哪里。默认情况下，处理器可通过 `/mcp/:name` 访问。

```typescript
import { defineMcpHandler } from '@nuxtjs/mcp-toolkit/server'

export default defineMcpHandler({
  name: 'migration', // 处理器挂载在 /mcp/migration
})
```

### `version`（可选）

处理器的版本。默认为模块配置的版本。

```typescript
import { defineMcpHandler } from '@nuxtjs/mcp-toolkit/server'

export default defineMcpHandler({
  name: 'migration',
  version: '2.0.0',
})
```

### `route`（可选）

处理器的自定义路由。默认为 `/mcp/:name`。

::callout{icon="i-lucide-info" color="info"}
此选项仅用于**自定义处理器**。对于默认处理器覆盖（`index.ts`），请改用 `nuxt.config.ts` 中的 `mcp.route`。
::

```typescript
import { defineMcpHandler } from '@nuxtjs/mcp-toolkit/server'

export default defineMcpHandler({
  name: 'migration',
  route: '/api/mcp/migration', // 自定义路由
})
```

### `browserRedirect`（可选）

当浏览器访问处理器端点时重定向到的 URL。默认为模块配置的 `browserRedirect`。

```typescript
import { defineMcpHandler } from '@nuxtjs/mcp-toolkit/server'

export default defineMcpHandler({
  name: 'migration',
  browserRedirect: '/docs/migration',
})
```

### `middleware`（可选）

在请求处理前后拦截请求的函数。适用于身份验证、日志记录和设置上下文。

```typescript [server/mcp/custom.ts]
import { defineMcpHandler } from '@nuxtjs/mcp-toolkit/server'

export default defineMcpHandler({
  name: 'custom',
  middleware: async (event) => {
    event.context.userId = 'user-123'
  },
})
```

::callout{icon="i-lucide-book-open" color="primary"}
有关详细文档和示例，请参阅[中间件指南](/advanced/middleware)。
::

### `experimental_codeMode`（可选）

启用[代码模式](/advanced/code-mode)，让 LLM 在一次 JavaScript 执行中编排多个工具调用。传入 `true` 使用默认值，或传入一个选项对象：

```typescript
import { defineMcpHandler } from '@nuxtjs/mcp-toolkit/server'

export default defineMcpHandler({
  name: 'custom',
  experimental_codeMode: {
    progressive: true,
    memoryLimit: 128,
  },
})
```

代码模式需要 `secure-exec` 和 Node.js `>=18.16.0`。

::callout{icon="i-lucide-book-open" color="primary"}
有关完整文档、安全细节和配置选项，请参阅[代码模式指南](/advanced/code-mode)。
::

### `tools`（可选）

支持三种形式——请选择最符合你使用场景的一种：

::code-group

```typescript [自动（默认）]
// server/mcp/handlers/admin/index.ts
import { defineMcpHandler } from '@nuxtjs/mcp-toolkit/server'

// `tools` 省略 → handlers/admin/tools/ 下的每个工具
// 都会自动注册（文件夹约定）。
export default defineMcpHandler({
  middleware: requireAdmin,
})
```

```typescript [静态数组]
import { defineMcpTool, defineMcpHandler } from '@nuxtjs/mcp-toolkit/server'

const tool1 = defineMcpTool({ ... })
const tool2 = defineMcpTool({ ... })

export default defineMcpHandler({
  name: 'custom',
  tools: [tool1, tool2],
})
```

```typescript [动态函数]
import { defineMcpHandler, getMcpTools } from '@nuxtjs/mcp-toolkit/server'

export default defineMcpHandler({
  // 每个标记为 'searchable' 的工具，不受文件夹影响。
  tools: event => getMcpTools({ event, tags: ['searchable'] }),
})
```

::

::callout{icon="i-lucide-book-open" color="primary"}
有关文件夹约定，请参阅[多处理器组织](/handlers/organization)；有关临时过滤，请参阅[`getMcpTools`](/advanced/listing-definitions)。每个工具的 `enabled()` 保护都会自动应用——请参阅[动态定义](/advanced/dynamic-definitions)。
::

### `resources`（可选）

与[`tools`](#tools-optional)相同的形式：省略则使用文件夹约定自动归属，传入数组，或传入 `event => getMcpResources({ event, ... })`。

```typescript
import { defineMcpResource, defineMcpHandler } from '@nuxtjs/mcp-toolkit/server'

const resource1 = defineMcpResource({ ... })
const resource2 = defineMcpResource({ ... })

export default defineMcpHandler({
  name: 'custom',
  resources: [resource1, resource2],
})
```

### `prompts`（可选）

与[`tools`](#tools-optional)相同的形式：省略则使用文件夹约定自动归属，传入数组，或传入 `event => getMcpPrompts({ event, ... })`。

```typescript
import { defineMcpPrompt, defineMcpHandler } from '@nuxtjs/mcp-toolkit/server'

const prompt1 = defineMcpPrompt({ ... })
const prompt2 = defineMcpPrompt({ ... })

export default defineMcpHandler({
  name: 'custom',
  prompts: [prompt1, prompt2],
})
```

